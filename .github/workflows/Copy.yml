name: copyFilesfromRepo

on:
  workflow_dispatch:
  
  
defaults:
  run:
    shell: bash
env: 
  org: 'htlnxls'


jobs:
  CreateVariableFile:
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
    - uses: actions/checkout@v2
    - name: Checkout private actions
      uses: actions/checkout@v2
      with:
        repository: ${{ env.org}}/repo2
        path: ./
        token: ${{ secrets.Acc_key }}
