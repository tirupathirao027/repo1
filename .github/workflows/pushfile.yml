name: testCopyfile

#on: [push]

on:
  workflow_dispatch:
    inputs:
      body:
        required: true
        type: string        
  
  
jobs:
  job1:
    runs-on:  [windows-latest]
    steps:
      - name: test1
        id: test
        run: |
          write-output "${{inputs.body}}"
          $test="$(${{inputs.body}}| ConvertTo-Json -Compress)"
          $req=$test|ConvertFrom-Json
          $req.Account


