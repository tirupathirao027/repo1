name: testCopyfile

on: [push]

  
  
  
jobs:
  job1:
    runs-on:  [windows-latest]
    steps:
      - name: test1
        id: step1
        run: |
          write-output "${{ github.workflow_id }}"
          write-output " location => ${{inputs.location}}"
          write-output "sku  =>  ${{inputs.sku}}"
          
