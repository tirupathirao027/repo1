name: copy

on:
  workflow_dispatch:
  
  
  
jobs:
  job01:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: git diff and set output
        id: set_output
        run: |
          $f="success"
          write-output "TERMINUS_RELEASE=$f" >> $GITHUB_ENV

      - name: testing
        run: |
          write-output $test
        env:
          test: ${{ env.TERMINUS_RELEASE }}

          
          


 
